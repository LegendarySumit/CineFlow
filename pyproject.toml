[project]
name = "cineflow"
version = "0.1.0"
description = "Production Crisis Director for Film & Media"
requires-python = ">=3.10"
dependencies = [
    "google-generativeai>=0.3.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.0.0",
    "requests>=2.31.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
